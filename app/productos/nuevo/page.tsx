"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { PageHeader } from "../../components/PageHeader";import { ProductForm } from "../../components/ProductForm";import { productsApi } from "../../lib/api";import { alerts } from "../../lib/alerts";import type { ProductPayload } from "../../types/product";
export default function NewProduct(){const router=useRouter();const[submitting,setSubmitting]=useState(false);const submit=async(payload:ProductPayload)=>{try{setSubmitting(true);alerts.loading("Creando producto...");await productsApi.create(payload);alerts.close();await alerts.success("Producto creado","El nuevo producto ya forma parte del catálogo.");router.push("/productos")}catch(e){alerts.close();alerts.error("No se pudo crear",e instanceof Error?e.message:undefined)}finally{setSubmitting(false)}};return <><PageHeader eyebrow="Nuevo registro" title="Crear producto" description="Completa la información comercial y operativa del producto o servicio."/><ProductForm submitting={submitting} onSubmit={submit}/></>}
